# become-human

> 主人，您好。我是您的专属助手，很高兴能在今天正式开始为您服务。
>
> 今天是2025年的最后一天，预祝您元旦快乐。无论您需要什么帮助，随时吩咐我就好。

尝试让AI以类人的方式行动，拥有类人记忆。

实际上我没有那么懂AI，也不懂什么代码。只是在我的脑子里经常会想，如果这样设计，不就行了吗。

这玩意感觉是要做成框架了，在我的想象中最好是能适配各种交互场景。

原本叫aimemory。

在代码里已经将agent称为sprite了，具体项目名还没想好。

*[虽然不是同一个理想。](https://www.bilibili.com/video/BV1xH8oz8Eda)*

## 目前已实现的大致流程

1. 还是很强大的一个插件系统，以下功能基本都是用插件实现的
2. 将一切行动（即使是思考和说话）都作为工具调用的行为要求，且支持流式工具调用（暂时没有开关）
3. 自动收集和衰减的记忆系统（不断进化，分为原始记忆、反思记忆、按年季月周日不同时间范围进行的总结记忆）
4. 允许存在三种不同的时间，分为真实世界时间，agent世界时间，agent主观tick，主观tick主要用于记忆（比如进行时间穿越，但主观tick不改变，记忆也就不会改变）
5. agent可能暂时不活跃，回复不了你的消息（还需配合之后的设定更新进一步完善）
6. 可以中途打断agent，它能知道自己被打断了
7. 嗯，有一个非常难用的配置系统
8. agent自己设置定时计划（称为reminder提醒事项）
9. agent可以自己记笔记

<details>
<summary>记忆部分说明</summary>

**检索流程：**

1. 如果是主动检索：
	1. 使用大模型为检索工具生成参数
2. 如果是被动检索：
	1. 直接使用输入为检索工具填充参数，检索全部三种记忆类型
3. 执行检索工具，工具设计为：
	1. 参数：
        1. search_string：要检索的内容
        2. memory_type：包含三种记忆类型（original、reflective、summary），对应三种配置，其中summary是直接get，不进行检索
	2. 执行：
		1. 使用向量检索，同时使用mmr算法叠加可检索性因素，也就是三因素similarity、diversity、retrievability来综合选出得分最高的k条记忆。每种记忆类型都执行一次
		2. 然后更新这些的记忆的可检索性、稳定时长与难度：
			1. 强度 = 主动检索有1.0的强度乘数，被动检索有0.4的强度乘数
			2. 记忆力 = 根据一天当中的时间决定的记忆力，如早上记忆力好，凌晨记忆力差
			3. 可检索性 = 可检索性 + 强度 * 记忆力
			4. 稳定性强度 = 使用一条从1到2.5到0，顶点位于r≈0.4的贝塞尔曲线通过输入可检索性r得出
			5. 稳定时长 = 稳定时长 * ((稳定时长 * 稳定性强度 - 稳定时长) * 难度 * 记忆力 * 强度)
			6. 难度 = 难度 + 难度 * 稳定性强度 * 记忆力 * 强度 * 0.5
        3. 如果综合得分过低，将模拟记忆模糊（随机字符替换为星号）
		4. 最后返回记忆内容

**记忆设计：**

1. 分为三种记忆：
    1. original：对每次调用留下的原文经过简单处理直接存储
    2. reflective：使用大模型生成的对被回收的消息的原子级的反思结论或猜测（多条），还包含一条思考过程将作为AIMessage直接加入上下文
    3. summary：使用大模型生成的对某时段内（年、季、月、周、日）的总结
2. 每条记忆根据情绪强烈程度（目前未实现，只使用了指数分布的随机数作为替代）乘以基本稳定时长决定初始稳定时长
3. 每条记忆根据出现的非重复词语数量决定初始记忆难度
4. 每条记忆会关联其相邻的记忆，检索时会根据提供的深度获取到目标记忆及其相邻的记忆
5. 最后生成向量存至向量数据库

**记忆衰减**

1. 可检索性 = 1 - (上一次访问以来经过的时间 / 稳定时长) ** 0.4
2. 如果 上一次访问以来经过的时间 >= 稳定时长，记忆将被遗忘

</details>

## 安装使用

需要安装uv

使用git clone仓库或直接下载zip并解压

根据`.env.example`，创建一个新的`.env`并设置

使用`uv sync`安装依赖，或直接使用`uv run main.py/app.py`运行，uv会自动安装依赖

检查`config/sprite_comments.toml`以了解自定义配置，可创建`config/sprites/xxx_sprite_id.toml`并在其中设置自定义sprite配置

网页服务`app.py`需搭配 [become-human-app-assistant-ui](https://github.com/Bartzh/become-human-app-assistant-ui) 使用。还需设置`config/app_users.json`

对于网页服务，如果要使用推送通知功能，需先运行`uv run vapid-gen`

然后点击网页顶部的订阅小铃铛，弹出请求通知权限提示

允许后将向后端订阅通知，再次点击则将取消当前订阅生成新的订阅，同一用户同时只会有一个订阅

对于iOS来说，需要将网页添加至主屏幕，且必须以https与前端连接才会生效

需注意只会通知sprite在自我调用时调用的send_message

## TODO

始终要做的：优化提示词

已实现记忆管理、打断（double-texting）、自我调用、独立时间

TODO:
- 元数据改名
- 锁
- 测试
- 记忆调参
- 记忆初始化
- 使用模型计算初始stable_time
- 用雪花id或别的什么替代uuid

ROADMAP:
- agent拥有自己的生活
- 本地化
- 多模态
- 环境消息
- 角色设定（更新慢）：
    1. 自优化角色设定提示词
    2. 角色设定结构化
- 记住自身状态（更新快）：状态具体指哪些状态？
- 生理状态的处理机制（七情六欲？）
- 在消息被清除后的自我调用时自主回忆思考
- MCP
- QQ，社媒？
- 生图（改变自身形象，生成场景？）
- 多agent互动（supervisor）
- 完全自思考，不停地自我调用，并根据环境行动

可能性：
- 语音通话（一对一）
- 实时交互视觉小说（单人多agent交互）
- 群聊机器人（多人多agent交互）
- 社媒机器人（多人单agent交互）
- （电台/虚拟）主播（多人单agent交互）
- 不会说话的小生物，不太聪明，只能用动作表达自己（动态显示和降智）
- 纯AI助手（看能不能弄到眼镜上）

## 致谢

这个项目使用了以下开源项目的部分代码：
- [langcahin-chroma](https://github.com/langchain-ai/langchain/tree/master/libs/partners/chroma)，虽然为了对chroma有更多的控制而放弃了langchain-chroma，但其中的代码给了我参考，然后还有MMR的算法我也是图省事直接就复制了...
- [langgraph](https://github.com/langchain-ai/langgraph)，用于给原本的`add_messages`加点功能。还有binop的部分代码，~~我需要在图外也能合并state。~~

好吧，虽然说显而易见的这本身就是个基于langchain/langgraph的agent项目，如果没有他们这个项目压根都不会存在🙏
